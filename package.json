{
  "name": "game-planner-portfolio",
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.4",
    "astro": "^6.1.0",
    "fuse.js": "^7.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.78.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
